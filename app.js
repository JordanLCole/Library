'use strict'

//buttons

const newBook = document.querySelector('')
const addBook = document.querySelector('')
const closeModal = document.querySelector('')
const deleteBook = document.querySelectorAll('')

//interactable elements



//library array

let myLibrary = [];

//constructor

function Book(author, title, pages, read, genre) {
    this.author = author;
    this.title = title;
    this.pages = pages;
    this.read = read;
    this.genre = genre;
}

//functions

function addToLibrary() {
    
}

function displayLibrary () {
    for(i = myLibrary.length, i > myLibrary.length, i++);
    
}